import React from 'react';
import { storiesOf } from '@storybook/react'; // eslint-disable-line
import Search from '.';

storiesOf('Search', module)
  .add('default', () => <Search />); // eslint-disable-line
